# Sistema Aprender v1 (Arquivado)

Este diretório contém o snapshot completo da **versão 1** do sistema. Ele
permanece somente para referência histórica. Não realize alterações aqui.

- Código Django v1 (`aprender_sistema/`, `core/`, `ingestao/`, etc.)
- Docker/infra antiga (`docker-compose.yml`, scripts)
- Documentação e relatórios legados

A versão ativa e mantida encontra-se em `../../v2/`.
