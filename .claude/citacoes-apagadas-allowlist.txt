# Arquivos de instrucao perdoados pelo detector de "citacao a caminho apagado"
# (v2/backend/scripts/check_agent_instructions.py).
#
# Este arquivo existe porque o gate nasce sobre divida preexistente. O padrao
# «limpar primeiro, trancar depois com allowlist» e o que este repositorio prova
# que funciona (check-no-legacy-js); trancar antes de limpar deixa o gate
# vermelho no dia 1 e ele e desligado antes de pegar o primeiro caso novo.
#
# Caminho relativo a raiz do repo, um por linha. Toda entrada precisa de motivo.
# Entrada sem motivo e o comeco da erosao — o gate volta a nao valer nada.
#
# Criado em 2026-08-25.

# ---------------------------------------------------------------------------
# PERMANENTE — declaracao historica correta, nao instrucao desatualizada.
#
# `src/api.ts` foi mesmo apagado (#1045, remocao do axios). A frase que o cita e
# "There is no `src/api.ts` or `httpClient.ts` — the single wrapper is
# `config.ts`", ou seja, o texto esta CERTO justamente por falar de um arquivo
# que nao existe mais. O discriminador de git nao consegue separar isso de
# instrucao que ficou para tras; esta e a saida prevista para essa classe.
# ---------------------------------------------------------------------------
.claude/skills/deprecation-and-migration/SKILL.md
.agents/skills/deprecation-and-migration/SKILL.md
