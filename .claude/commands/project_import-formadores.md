---
allowed-tools: Bash(python manage.py:*), Bash(git add:*), Bash(git commit:*)
description: Run the Formadores import script and summarize results
---

Run: `python manage.py runscript core.scripts.import_formadores` and capture the created/existing counts. Update docs if needed.
(think)
